<!-- Navigation -->
<nav>
    <a href="<%- config.root %>"><%- theme.menu_title || config.title || "" %></a>

    <ul>
        <% for (var i in theme.menu){ %>
            <li>
                <a href="<%- theme.menu[i].url ? url_for(theme.menu[i].url) : url_for(theme.menu[i]) %>">
                    <%= i %>
                </a>
            </li>
        <% } %>
    </ul>
</nav>